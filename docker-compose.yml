services:
  mini-game-hub-web:
    build:
      context: .
    image: mini-game-hub-web:local
    ports:
      - "8081:8081"
    volumes:
      - mini-game-hub-scores:/data
    restart: unless-stopped

volumes:
  mini-game-hub-scores:
